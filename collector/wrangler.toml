name = "samai-survey"
main = "worker.js"
compatibility_date = "2025-09-01"

# Where responses wait until the workflow collects them. The Worker holds no
# GitHub credential; see the header of worker.js.
[[kv_namespaces]]
binding = "RESPONSES"
id = "635436eb93aa4f8cb857e251e5bc9d7d"

[vars]
# Only this origin may post. Add more, comma-separated, if the course is
# also served from a custom domain.
ALLOWED_ORIGIN = "https://omz007.github.io"
