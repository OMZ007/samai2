# Survey responses

Written here by the collector Worker, one JSON file per response, under
responses/module-<n>/.

This branch is deliberately NOT main: GitHub Pages builds from main, so nothing
here is served by the website and a response never triggers a site rebuild.
